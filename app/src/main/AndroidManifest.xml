<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to use the camera -->
    <uses-permission android:name="android.permission.CAMERA" />



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Technology">

        <!-- MainActivity is launcher -->
        <activity
            android:name="com.sharwin.technology.MainActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declare CameraActivity -->
        <activity
            android:name="com.sharwin.technology.LearnActivity"
            android:screenOrientation="portrait"
            android:exported="true" />

        <activity android:name="com.sharwin.technology.ColorDetectorActivity"
            android:screenOrientation="portrait"
            android:exported="true" />

        <activity android:name="com.sharwin.technology.FaceDetectorActivity" />
        <activity android:name="com.sharwin.technology.QuizActivity" />
        <activity android:name="com.sharwin.technology.VideoActivity" />

        <activity android:name="com.sharwin.technology.DragMatchActivity" />
        <activity android:name="com.sharwin.technology.FilterBuilderActivity" />
        <activity android:name="com.sharwin.technology.ThankYouActivity" />





    </application>


</manifest>
